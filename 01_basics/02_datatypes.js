"use strict"        // treat all JS code as newer version

// alert(3 + 3)     // we are using nodejs, not the web browser

/*
Types of DataTypes in JS:

    * Primitive DataTypes:
        - number -> 2 to power 53
        - bigint
        - string => ""
        - boolean => true/false
        - null => standalone value
        - undefined => No value is assined yet
        - symbol => to define uniqueness
        - object
*/

console.log(typeof "Vishal");       // datatype will be String
console.log(typeof undefined);      // undefined
console.log(typeof nul);            // onject